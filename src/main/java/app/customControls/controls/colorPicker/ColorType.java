package app.customControls.controls.colorPicker;

/**
 * ColorTypes make up the {@link ColorFormat ColorFormats} supported by a {@link MaterialColorPicker}
 */
public enum ColorType {
    HEX,
    RED,
    GREEN,
    BLUE,
    HUE,
    SATURATION,
    VALUE,
    NULL
}
